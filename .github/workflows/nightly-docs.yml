name: nightly-docs (stub)

on:
  # Disabled automated doc generation to keep heavy operations local-only.
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Intentional No-Op
        run: |
          echo "Nightly docs build disabled. Generate docs locally with:"
          echo "  just check-docs"
          echo "  just arch-docs"
          echo "Then publish or attach artifacts manually as needed."
